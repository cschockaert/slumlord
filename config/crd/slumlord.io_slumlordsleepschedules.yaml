---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: slumlordsleepschedules.slumlord.io
spec:
  group: slumlord.io
  names:
    kind: SlumlordSleepSchedule
    listKind: SlumlordSleepScheduleList
    plural: slumlordsleepschedules
    singular: slumlordsleepschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.suspend
      name: Suspended
      type: boolean
    - jsonPath: .status.sleeping
      name: Sleeping
      type: boolean
    - jsonPath: .spec.schedule.start
      name: Start
      type: string
    - jsonPath: .spec.schedule.end
      name: End
      type: string
    - jsonPath: .status.daysDisplay
      name: Days
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SlumlordSleepSchedule is the Schema for the SlumlordSleepSchedules
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SlumlordSleepScheduleSpec defines the desired state of SlumlordSleepSchedule
            properties:
              schedule:
                description: Schedule defines when workloads should sleep
                properties:
                  days:
                    description: |-
                      Days specifies which days the schedule applies (0=Sunday, 6=Saturday)
                      If empty, applies every day
                    items:
                      type: integer
                    maxItems: 7
                    type: array
                  end:
                    description: End is the time to wake up (cron format for time,
                      e.g., "06:00")
                    pattern: ^([01]?[0-9]|2[0-3]):[0-5][0-9]$
                    type: string
                  start:
                    description: Start is the time to start sleeping (cron format
                      for time, e.g., "22:00")
                    pattern: ^([01]?[0-9]|2[0-3]):[0-5][0-9]$
                    type: string
                  timezone:
                    description: |-
                      Timezone is the timezone for the schedule (e.g., "Europe/Paris", "UTC")
                      Defaults to UTC if not specified.
                    type: string
                required:
                - end
                - start
                type: object
              selector:
                description: Selector specifies which workloads to target
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: MatchLabels selects workloads by labels
                    type: object
                  matchNames:
                    description: MatchNames selects workloads by name (supports wildcards)
                    items:
                      type: string
                    type: array
                  types:
                    description: |-
                      Types specifies which workload types to target
                      Valid values: Deployment, StatefulSet, CronJob, Cluster, HelmRelease, Kustomization, ThanosRuler, Alertmanager, Prometheus, MariaDB, MaxScale
                    items:
                      type: string
                    type: array
                type: object
              suspend:
                description: |-
                  Suspend prevents the schedule from running when true.
                  If workloads are currently sleeping, they will be woken up.
                type: boolean
            required:
            - schedule
            - selector
            type: object
          status:
            description: SlumlordSleepScheduleStatus defines the observed state of
              SlumlordSleepSchedule
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              daysDisplay:
                description: DaysDisplay is a human-readable representation of the
                  scheduled days
                type: string
              lastTransitionTime:
                description: LastTransitionTime is the last time the sleep state changed
                format: date-time
                type: string
              managedWorkloads:
                description: ManagedWorkloads lists the workloads being managed
                items:
                  description: ManagedWorkload tracks a workload managed by this schedule
                  properties:
                    kind:
                      description: Kind is the workload kind (Deployment, StatefulSet,
                        CronJob, Cluster, HelmRelease, Kustomization, ThanosRuler,
                        Alertmanager, Prometheus, MariaDB, MaxScale)
                      type: string
                    name:
                      description: Name is the workload name
                      type: string
                    originalHibernation:
                      description: OriginalHibernation stores the hibernation annotation
                        value before sleeping (for CNPG Cluster)
                      type: string
                    originalReplicas:
                      description: OriginalReplicas stores the replica count before
                        sleeping (for Deployment/StatefulSet)
                      format: int32
                      type: integer
                    originalSuspend:
                      description: OriginalSuspend stores the suspend state before
                        sleeping (for CronJob, HelmRelease, Kustomization, MariaDB,
                        MaxScale)
                      type: boolean
                  required:
                  - kind
                  - name
                  type: object
                type: array
              sleeping:
                description: Sleeping indicates whether workloads are currently sleeping
                type: boolean
            required:
            - sleeping
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
